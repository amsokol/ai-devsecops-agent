Live check of review publishing.

This file exists only so a scratch pull request has a diff to comment on.
It is removed when the check is done.

A second line added on the second pass, so a fresh thread has somewhere to attach.
